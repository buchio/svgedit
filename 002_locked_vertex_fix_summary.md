
# 修正の概要: ロックされた頂点の隣接点をドラッグした際の移動防止

## 問題
ロックされた頂点（`locked=true`）の隣接点（`prev` または `next`）を移動させた際、ロックされているはずの頂点の座標まで一緒に移動してしまうというバグがありました。

## 解決策
`packages/svgcanvas/core/path-method.js` の `Segment.prototype.move` メソッドにおいて、`mate` （サブパスの開始点である M セグメント）の位置を更新するロジックにガードを追加しました。

具体的には、以下の条件を追加しました：
```javascript
if (this.mate && !this.mate.locked) { ... }
```
これにより、`mate` がロックされている場合は、その座標更新処理がスキップされ、位置が維持されるようになります。

## 検証結果
ブラウザ内でのテストにより、以下の挙動を確認しました：
1.  **左側の隣接点（Vertex 6）をドラッグ**: ロックされた頂点（Vertex 0 / Index 7）は移動せず、座標 `(322, 159)` を維持しました。
2.  **右側の隣接点（Vertex 1）をドラッグ**: ロックされた頂点（Vertex 0）は移動せず、座標 `(322, 159)` を維持しました。

この修正により、意図せずロックされた頂点が動いてしまう問題が解消されました。
